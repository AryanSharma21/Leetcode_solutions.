class Solution {
    public int[] runningSum(int[] nums) {
        int result=0;
        int [] output = new int[nums.length];
        for(int i = 0;i<nums.length;i++){
            result += nums[i];
            output[i] = result;
        }
        return output;
    }
}
